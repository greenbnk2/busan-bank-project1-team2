<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 🔥 여기에 namespace 선언 -->
<mapper namespace="kr.co.bnkfirst.mapper.EmployeeMapper">

    <!-- SQL들... -->
    <select id="findAllEmployees" resultType="kr.co.bnkfirst.dto.corporate.employee.EmployeeListDto">
        SELECT EMPID, NAME, PLANTYPE, ACCOUNTNO, STATUS
        FROM EMPLOYEE
        ORDER BY EMPID
    </select>

</mapper>
