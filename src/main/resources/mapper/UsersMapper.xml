<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.bnkfirst.mapper.UsersMapper">

    <!-- member_main 로그인 -->
    <select id="findUserById" parameterType="string" resultType="kr.co.bnkfirst.dto.UsersDTO">
        SELECT
            MID     AS mid,
            MPW     AS mpw,
            MNAME   AS mname,
            MPHONE  AS mphone,
            MEMAIL  AS memail,
            MDATE   AS mdate,
            MGRADE  AS mgrade
        FROM USERS
        WHERE MID = #{mid}
    </select>

    <!-- member_info 정보입력 -->
    <insert id="insertUser" parameterType="kr.co.bnkfirst.entity.Users">
        INSERT INTO USERS (
            "uId", MID, MPW, MNAME, MGENDER, MJUMIN,
            MPHONE, MCARRIER, MBIRTH, MEMAIL, MADDRESS,
            MDATE, MGRADE, MCOND
        )
        VALUES (
                   SEQ_USERS.NEXTVAL,
                   #{mid}, #{mpw}, #{mname}, #{mgender}, #{mjumin},
                   #{mphone}, #{mcarrier}, #{mbirth, jdbcType=DATE},
                   #{memail, jdbcType=VARCHAR}, #{maddress, jdbcType=VARCHAR},
                   SYSDATE, 'Family', '활동중'
               )
    </insert>

    <!-- member_info 아이디 중복확인 -->
    <select id="existsByMid" parameterType="string" resultType="int">
        SELECT COUNT(*)
        FROM USERS
        WHERE "MID" = #{mid}
    </select>

</mapper>
